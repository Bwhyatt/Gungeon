import random

def generate_Dungeon(RoomList):
    for i in range(len(RoomList)):
        for j in range(len(RoomList[i].exits)): #exits is like [1, 2] for left and up
            RandomConnectedRoom = random.randint(0, len(RoomList))
            #might have to do -1
            hascommon = not set(RoomList[i].exits).isdisjoint(RoomList[RandomConnectedRoom].exits)
            WhileCounter = 0
            while not hascommon and WhileCounter < 4:
                WhileCounter += 1
                RandomConnectedRoom = random.randint(0, len(RoomList))
                hascommon = not set(RoomList[i].exits).isdisjoint(RoomList[RandomConnectedRoom].exits)
                #break out of loop eventually
            #make the exits simply generate
            #I need something that will determine if a room already has the max number of exits
    return NewRoomList